# Sink Camera Id update

- The python will add the camera ID automatically to the client integration config
- It feteches the cam Id from sink dashboard config file and adds in the client config file
